# My1stRepository

A small Python command-line tool to check password strength and generate secure passwords.

## Usage

    python passtool.py check "MyPassw0rd!"
    python passtool.py gen 20

## Tests

    python -m unittest

## Notes

Uses Python's `secrets` module for generation. No external dependencies.
